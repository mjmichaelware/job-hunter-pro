# /security-scan

Use security-auditor and scheduler-oidc-engineer.

Check for hardcoded secrets, printed secrets, URL tokens, broken /api/ingest auth, unsafe Scheduler config, and accidental live quota burn.
Do not print secret values.
