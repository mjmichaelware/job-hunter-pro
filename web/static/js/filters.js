document.addEventListener('DOMContentLoaded',()=>{const ind=document.getElementById('filter-industry');const rad=document.getElementById('filter-radius');const mat=document.getElementById('filter-match');ind.addEventListener('change',(e)=>{AppState.filters.industry=e.target.value;applyLocalFilters()});rad.addEventListener('input',(e)=>{AppState.filters.radius=parseInt(e.target.value);applyLocalFilters()});mat.addEventListener('input',(e)=>{AppState.filters.matchScore=parseInt(e.target.value);applyLocalFilters()})});function applyLocalFilters(){if(AppState.activeTab==='live_jobs')renderJobsList(AppState.cachedData.jobs);if(AppState.activeTab==='opportunities')renderOpportunitiesList(AppState.cachedData.opportunities)}